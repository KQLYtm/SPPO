@model IEnumerable<FormVM>

<table class="table table-hover">
    <thead>
       <tr>
       <th>Ime</th>
       <th>Prezime</th>
       </tr>
    </thead>
    <tbody>
        @foreach (var x in Model)
        {
        <tr>
            <td>
                @Html.ActionLink(x.Cv, "GetCv", "Form", new { CvName = x.Cv }, null)
            </td>
        </tr>
        }
    </tbody>
</table>